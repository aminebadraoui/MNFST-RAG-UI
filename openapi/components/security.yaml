components:
  securitySchemes:
    BearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT
      description: JWT Bearer token for authentication

  securityRequirements:
    BearerAuth:
      - BearerAuth: []
    
    OptionalBearerAuth:
      - []
    
    SuperadminOnly:
      - BearerAuth: []
      
    TenantAdminOnly:
      - BearerAuth: []
      
    AuthenticatedUser:
      - BearerAuth: []